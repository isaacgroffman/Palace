# Python packages for the Pitch Profiler bundle (R/05_promodel.R via reticulate).
# Ranges, not pins: Connect Cloud runs Python 3.11 and picks compatible builds.
lightgbm>=4.5,<5
polars>=1.0
pandas>=2.2
numpy>=1.26,<3
pyarrow>=16.0
